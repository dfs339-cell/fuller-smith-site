baseURL = "https://www.fuller-smith.co.uk/"
languageCode = "en-gb"
title = "Daniel Fuller-Smith — Local AI, Self-Hosted"
theme = "PaperMod"

# Pagination
pagination.pagerSize = 10

# Enable Git info (last-modified dates on posts, optional)
enableGitInfo = true

# Publish posts even if their timestamp is technically still "in the future"
# relative to build time — needed since scrape-pipeline posts get staggered
# timestamps within the same day.
buildFuture = true

[params]
  description = "Self-hosting AI on your own hardware: reviews, guides, and the build log for the Sovereign AI Lab."
  author = "Daniel Fuller-Smith"
  defaultTheme = "auto"        # auto | light | dark
  ShowReadingTime = true
  ShowShareButtons = false
  ShowPostNavLinks = true
  ShowBreadCrumbs = true
  ShowCodeCopyButtons = true
  ShowToc = true
  TocOpen = false
  disableSpecial1stPost = false
  hidemeta = false

  # Restrict the homepage feed to News and Articles — without this, PaperMod
  # picks whichever section has the most pages, which became the Book once
  # Volume 1 was added.
  mainSections = ["news", "posts"]

  [params.homeInfoParams]
    Title = "Daniel Fuller-Smith"
    Content = "Guides and hardware breakdowns for running your own AI, locally. No cloud, no subscriptions, no data leaving your machine."

[[menu.main]]
  identifier = "news"
  name = "News"
  url = "/news/"
  weight = 5

[[menu.main]]
  identifier = "posts"
  name = "Articles"
  url = "/posts/"
  weight = 10

[[menu.main]]
  identifier = "book"
  name = "Book"
  url = "/book/"
  weight = 15

[[menu.main]]
  identifier = "about"
  name = "About"
  url = "/about/"
  weight = 20

[markup]
  [markup.highlight]
    noClasses = false
    style = "monokai"
  [markup.goldmark.renderer]
    unsafe = true   # allows raw HTML in markdown, e.g. embedded tables/widgets later

[taxonomies]
  tag = "tags"
  series = "series"

[outputs]
  home = ["HTML", "RSS", "JSON"]   # JSON output enables client-side search later if wanted
